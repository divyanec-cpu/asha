/**
 * The support address, in one place.
 *
 * Named here rather than typed into each page because it appears in the privacy
 * policy, the terms, and the help screen — and a contact address that disagrees
 * with itself across those three is worse than none, since the privacy policy's
 * copy is the one a data-protection complaint would actually rely on.
 *
 * This is the builder's personal address, used during the trial. Replacing it
 * with a dedicated support address is an open pre-launch item (CLAUDE.md), and
 * doing so should be a one-line change here.
 */
export const CONTACT_EMAIL = "divyanec@gmail.com";
